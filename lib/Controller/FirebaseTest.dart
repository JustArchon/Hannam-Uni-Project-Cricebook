import 'package:flutter/material.dart';

class FirebaseTest extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Circe Book Firebase Test Page')),
      body: Container()
    );
  }
}