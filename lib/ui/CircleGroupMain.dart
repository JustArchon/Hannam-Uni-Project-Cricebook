import 'package:flutter/material.dart';

class CircleGroupMain extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Circe Book Group Main')),
      body: Container()
    );
  }
}