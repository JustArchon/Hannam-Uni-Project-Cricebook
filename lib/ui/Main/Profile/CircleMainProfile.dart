import 'package:flutter/material.dart';

class CircleMainProfile extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Circe Book Main Profile')),
      body: Container()
    );
  }
}