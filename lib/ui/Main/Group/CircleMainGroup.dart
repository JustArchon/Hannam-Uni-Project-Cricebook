import 'package:flutter/material.dart';

class CircleMainGroup extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Circe Book Main Group')),
      body: Container()
    );
  }
}